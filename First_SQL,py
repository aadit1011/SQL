CREATE TABLE IF NOT EXISTS Student(
     student_id INT PRIMARY KEY,
     name VARCHAR(50),   
     major VARCHAR(20)
);
CREATE TABLE IF NOT EXISTS Student(
     student_id INT PRIMARY KEY,
     name VARCHAR(50),   
     major VARCHAR(20)
);
